// ─── DemoPlayer (deprecated) ─────────────────────────────────────────────────
// Replaced by:
//   - Direct YouTube deep-link from LogPage's video button (window.open).
//   - PhotoView.jsx for inline photo display with replace/remove.
// Kept as an empty module so old imports (if any survive in a worktree) don't
// break the build. Safe to delete this file in a future cleanup.

export default function DemoPlayer() { return null; }
